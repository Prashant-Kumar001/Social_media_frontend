import React from 'react'
import { useNavigate } from 'react-router-dom';

const notification = ({t, messages}: {t: any, messages: any}) => {

    const navigate = useNavigate();
  return (
    <div>
      
    </div>
  )
}

export default notification
